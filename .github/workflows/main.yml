# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Install Modules
        run: npm i
      - name: Install ESLint
        run: npm i eslint --save-dev
      - name: Run ESLint
        run: npx eslint . --ext .js,.jsx,.ts,.tsx
      - name: On Fail
        if: ${{ failure() }}
        run: 'curl -X POST https://discord.com/api/webhooks/829192633649594408/zPum--UFktCMsUK5dh4pzMu7KjmEEJApU86d2icZTpMPUSSBN7iJCx5SAVZvOhY01cWg -H "Content-Type: application/json" --data "{\"content\": \"<@416147488643481610>\", \"embeds\": [{\"title\": \"nice fucking code bro\", \"color\":16716544,\"description\":\"<:no:756112149302083635> CoreCoder-Devs/CoreCoder-Studio: CI Failing\"}]}"'
      - name: On success
        if: ${{ success() }}
        run: 'curl -X POST https://discord.com/api/webhooks/829192633649594408/zPum--UFktCMsUK5dh4pzMu7KjmEEJApU86d2icZTpMPUSSBN7iJCx5SAVZvOhY01cWg -H "Content-Type: application/json" --data "{\"embeds\": [{\"title\": \"nice fucking code bro\", \"color\":2162432,\"description\":\"<:happy:701724430061273138> CoreCoder-Devs/CoreCoder-Studio: CI Passing\"}]}"'
